Liste struct à faire : 
	- Statistiques(pv,armure,force,vitesse,vision,niveau)
	- coord(x,y)
	- joueur(coord,inventaire,stat)
	- monstre(coord,pv,entier,degats,vision,xp,id_drop)
	- item(id,nom,description,sur_carte,coord,stat_item)
	- inventaire(taille_inv,taille_slots_armure,taille_slots_main,inv,main,armure)


struct Coord {
    int x;
    int y;
}; 


struct Statistiques {
    int pv;
    int armure;
    int force;
    int vitesse;
    int vision;
    int niveau;
};

struct item {
    int id;
    std::string nom;
    std::string description;
    bool sur_carte;
	bool stackable;
	bool consommable;
    Coord coordonnee
    int stat_item[5]
};
    
struct inventaire {
    static const int taille_inv = 5; #valeur a réfléchir
    static const int taille_slots_armure = 3; #valeur a réfléchir
    static const int taille_slots_main = 2; #valeur a réfléchir
    Item inv[TAILLE_INV];
    Item main[TAILLE_SLOTS_MAIN];
    Item armure[TAILLE_SLOTS_ARMURE];
};

struct monstre {
    Coord coordonnee;
    int pv
    int degats;
    int vision;
    int xp;
    int id_drop
};


struct joueur {
    Coord coordonnee;
    Inventaire inventaire
    Statistiques stats
};

// a finir 
struct carte {
    static const int nb_murs = 5; #valeur a réfléchir
    static const int nb_portes = 3; #valeur a réfléchir
    static const int nb_monstres = 2; #valeur a réfléchir
    static const int nb_items = 2; #valeur a réfléchir
    Coord mur[nb_murs];
    porte main[nb_portes];
    Item armure[nb_monstres ];
};
//


